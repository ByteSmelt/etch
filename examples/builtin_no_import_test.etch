// Test that builtin functions work without imports

fn main() {
  // All builtins should work without any imports
  print(42);

  seed(123);
  let r = rand(10);
  print(r);

  let s: string = toString(100);
  let opt: option[int] = parseInt(s);
  if isSome(opt) {
    print(100);
  }
}